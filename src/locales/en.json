{
  "hello": "Hello, ${name}! \nSend me any location to get wewather data.",
  "lang_changed": "Language changed successfully",
  "send_location_btn": "📍 Send location",
  "weather_message": "<b>Currently</b>\n\n${currently.summary}\nTemperature: ${Number(currently.temperature).toFixed(1)} °${flags.units === 'si' ? 'C' : 'F'} (feels like ${Number(currently.apparentTemperature).toFixed(1)} °${flags.units === 'si' ? 'C' : 'F'})\nWind speed: ${Number(currently.windSpeed).toFixed(1)} m/s"
}
